<!-- Each file has a different responsibility:

schemas/ → defines what data comes in and goes out (validation).
routers/ → defines API endpoints like /register.
services/ → contains the business logic (checking duplicates, hashing passwords, saving users). -->